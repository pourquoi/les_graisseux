liip_imagine:
  resolvers:
    default:
      flysystem:
        filesystem_service: upload.storage
        root_url:           '%env(SCHEME)%://%env(HOST)%/test/upload'
        cache_prefix:       cache
        visibility:         public